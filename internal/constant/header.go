package constant

const (
	HEADER_TRACE_ID = "X-Trace-ID" // Header for trace id, trace id is used to trace for each request
	HEADER_LOCALE   = "X-Locale"   // Header for locale, locale is used to determine the language of response
	HEADER_TRACE    = "X-Trace"    // Header for trace objects, which is used to trace the execution of the requests
)
